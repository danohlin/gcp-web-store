apiVersion: v2
name: web-store
description: Storefront API and SPA for GKE Autopilot, fronted by a Cloud Load Balancer.
type: application

# Chart version is bumped by hand; appVersion tracks the container image tag and
# is overridden by CI at deploy time.
version: 0.1.0
appVersion: '0.1.0'

maintainers:
  - name: danohlin
